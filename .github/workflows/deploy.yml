name: Deploy

on:
  push:
    branches:
      - TOTEM_GRUPO2
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      ref:
        description: Branch, tag or commit to deploy
        required: false
        default: TOTEM_GRUPO2

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || github.ref }}

      - name: Prepare optional compose files
        run: |
          mkdir -p Backend/synchronizer
          touch Backend/synchronizer/.env Backend/synchronizer/.last_fetch.json
 
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate Compose file
        run: docker compose -f Docker/compose.prod.yml config

      - name: Build deployment stack
        run: docker compose -f Docker/compose.prod.yml build --parallel
