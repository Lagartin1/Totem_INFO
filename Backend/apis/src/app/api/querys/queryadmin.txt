usar con la imagen docker de la carpeta Docker

para solo correr la imagen de mongo hacer :

  sudo docker compose -f componse.prod.yml up -d mongo



docker exec -it docker-database_mongo-1 mongosh \
  -u admin -p test123 --authenticationDatabase admin \
  --eval "db.getSiblingDB('totem_admins').Usuario.updateOne(
    { username: 'admin' },
    {
      \$setOnInsert: {
        nombre: 'Syncronizer',
        apellido: 'system',
        email: 'sync@example.com',
        username: 'syncronizer',
        authoriced: true,
        password: '\$2b\$10\$rD9O4AsOI86kzAJNb0sDo.qKkwuyo13N00JSwH8AimBgsZ1SRCZKa',
        createdAt: new Date(),
        updatedAt: new Date(),
        isActive: true,
        failedLoginAttempts: 0
      }
    },
    { upsert: true }
  )"
